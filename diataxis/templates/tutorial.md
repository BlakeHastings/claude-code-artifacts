<!--
TUTORIAL template (learning-oriented). A lesson for a newcomer who learns by doing.
Rules: perfect reliability, visible results early, zero decisions for the learner, no theory,
no alternatives. Link out for explanation. Voice: "we will…", "you will see…".
Delete these comments as you fill it in.
-->

# Build your first <thing>

In this tutorial we will <build/deploy the concrete artifact>. By the end you will have a
working <result> and have seen <the key moments> happen.

You do not need to understand <topic> first; you will pick it up by doing. For the reasoning
behind it, see [<explanation page>](...).

## Before you start

- <Exact prerequisite, e.g. Node 20+ installed>
- <Exact prerequisite>

## Step 1: <concrete first action with an early visible result>

Run:

```sh
<exact command>
```

You will see:

```
<exact expected output>
```

Notice that <the thing to observe>.

## Step 2: <next concrete action>

<exact instruction + exact expected result>

## Step 3: <next concrete action>

<...repeat: each step small, each result visible...>

## What we built

You now have <the working result>. We <one-sentence recap of the journey>.

Next, you can follow [<a related how-to guide>](...) to <real task>, or read
[<the explanation>](...) to understand how it works.
