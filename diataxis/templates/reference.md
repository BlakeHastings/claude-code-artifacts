<!--
REFERENCE template (information-oriented). Neutral, complete description a user consults.
Rules: describe and ONLY describe. Mirror the product's structure. Consistent format for every
entry. Examples illustrate, they do not instruct. No why, no recipes, no opinion: link out.
Delete these comments as you fill it in.
-->

# <Component / API / Command> reference

<One neutral sentence stating what this is.>

## <Entry name, e.g. function/class/endpoint/option>

<One-line factual description.>

| Field | Type | Default | Description |
| --- | --- | --- | --- |
| `<name>` | `<type>` | `<default>` | <factual description> |
| `<name>` | `<type>` | `<default>` | <factual description> |

**Returns:** `<type>` - <what it is>.

**Errors / raises:** `<Error>` when <condition>.

**Example:**

```
<minimal illustrative usage, no narration>
```

## <Next entry, same format>

<...repeat the identical structure for every entry; keep ordering consistent...>

<!-- To learn how to use these together, link to a how-to guide. For rationale, link to an explanation. -->
